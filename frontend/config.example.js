/**
 * Copy this file to config.js and set your API Gateway base URL.
 * config.js is gitignored — do not commit secrets or environment-specific URLs.
 *
 * Shape:
 *   window.APP_CONFIG = {
 *     apiUrl: "https://{api-id}.execute-api.{region}.amazonaws.com"
 *   };
 *
 * No trailing slash on apiUrl.
 */
window.APP_CONFIG = {
  apiUrl: "https://YOUR_API_ID.execute-api.eu-north-1.amazonaws.com",
};
